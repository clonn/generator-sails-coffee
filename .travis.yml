language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sails-coffee' ]; then cd .. && eval "mv $currentfolder generator-sails-coffee" && cd generator-sails-coffee; fi
